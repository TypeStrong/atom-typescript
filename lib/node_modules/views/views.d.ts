
// I find it easiest to create Views in CoffeeScript
// This is because of the way that Atom works
// This means that views a losely coupled with the API which we declare here
// But atleaset the views are readable

declare module 'views/tooltip-view'{
	export var TooltipView: { new (rect: any): ITooltipView; }
	export interface ITooltipView {
		updateText(text: string);

		// Methods from base View
		remove();
	}
}
