#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../atom-typescript/dist/main/bin/build" "$@"
  ret=$?
else 
  node  "$basedir/../atom-typescript/dist/main/bin/build" "$@"
  ret=$?
fi
exit $ret
