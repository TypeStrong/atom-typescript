#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../testla/bin/testla" "$@"
  ret=$?
else 
  node  "$basedir/../testla/bin/testla" "$@"
  ret=$?
fi
exit $ret
